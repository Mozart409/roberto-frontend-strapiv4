version: 1
frontend:
  phases:
    preBuild:
      commands:
      - corepack enable
      - pnpm install --prod
    build:
      commands:
      - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
    - '**/*'
  cache:
    paths:
    - .next/cache/**/*
    - node_modules/**/*
