pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx}"
      run: biome format --write {staged_files}
      stage_fixed: true
commit-msg:
  jobs:
    - name: cog verify
      run: cog verify --file {1}
